name: Deploy

# Deploy FAQAT PR merge bo'lganda (to'g'ridan-to'g'ri push deploy QILMAYDI).
#   main ga PR merge -> PROD serverga deploy
#   dev  ga PR merge -> DEV  serverga deploy
# workflow_dispatch — Actions'dan qo'lda qayta deploy (tanlangan branch bo'yicha).
on:
  pull_request:
    types: [closed]
    branches:
      - main
      - dev
  workflow_dispatch:

# Bir branchda bir vaqtda faqat bitta deploy ishlaydi
concurrency:
  group: deploy-${{ github.event.pull_request.base.ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  deploy-prod:
    if: >-
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'main')
      || (github.event_name == 'workflow_dispatch' && github.ref_name == 'main')
    runs-on: [self-hosted, prod]
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.ref || github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Prod env — prod serverdagi haqiqiy .env fayldan (git'ga tushmaydi).
      - name: Use prod server env file
        env:
          SERVER_ENV: /var/www/frontend/.env
        run: |
          test -f "$SERVER_ENV" || { echo "::error::$SERVER_ENV topilmadi"; exit 1; }
          cp "$SERVER_ENV" .env.production

      - name: Install dependencies
        env:
          # Ba'zi serverlarda IPv6 buzuq → npm ci osilib qoladi. IPv4 birinchi.
          NODE_OPTIONS: --dns-result-order=ipv4first
        run: npm ci --no-audit --no-fund --prefer-offline

      - name: Build (production)
        run: npm run build

      # Deploy target ham env'dan: prod server .env ichida DEPLOY_PATH=...
      - name: Publish to web root
        run: |
          TARGET=$(grep -E '^[[:space:]]*DEPLOY_PATH[[:space:]]*=' .env.production | tail -1 | cut -d= -f2- | tr -d '"'\'' ')
          if [ -z "$TARGET" ]; then
            echo "::error::DEPLOY_PATH .env ichida yo'q (prod server .env ga qo'shing)" && exit 1
          fi
          case "$TARGET" in
            /|/var/www|/var/www/|/home|/root|/etc*|/usr*)
              echo "::error::DEPLOY_PATH ($TARGET) xavfli — alohida frontend papka bo'lsin" && exit 1 ;;
          esac
          test -d "$TARGET" || { echo "::error::$TARGET papkasi yo'q"; exit 1; }
          rsync -a --delete dist/ "$TARGET"/

  # DEV: dev serverdagi self-hosted runnerda build + publish.
  # Env GitHub'dan emas — serverdagi mavjud /var/www/hrm_front/.env dan o'qiladi.
  # Hech qanday secret / commit kerak emas.
  deploy-dev:
    if: >-
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'dev')
      || (github.event_name == 'workflow_dispatch' && github.ref_name == 'dev')
    runs-on: [self-hosted, dev]
    environment: development
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.ref || github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Dev env — serverdagi haqiqiy .env fayldan (git'ga tushmaydi).
      - name: Use dev server env file
        env:
          SERVER_ENV: /var/www/hrm_front/.env
        run: |
          test -f "$SERVER_ENV" || { echo "::error::$SERVER_ENV topilmadi"; exit 1; }
          cp "$SERVER_ENV" .env.development

      - name: Install dependencies
        env:
          # Ba'zi serverlarda IPv6 buzuq → npm ci osilib qoladi. IPv4 birinchi.
          NODE_OPTIONS: --dns-result-order=ipv4first
        run: npm ci --no-audit --no-fund --prefer-offline

      - name: Build (development)
        run: npm run build:dev

      # Deploy target ham env'dan (server .env → .env.development) — GitHub Variable kerak emas.
      # Serverdagi /var/www/hrm_front/.env ichida: DEPLOY_PATH=/var/www/hrm_front/dist
      - name: Publish to web root
        run: |
          TARGET=$(grep -E '^[[:space:]]*DEPLOY_PATH[[:space:]]*=' .env.development | tail -1 | cut -d= -f2- | tr -d '"'\'' ')
          if [ -z "$TARGET" ]; then
            echo "::error::DEPLOY_PATH .env ichida yo'q (server /var/www/hrm_front/.env ga qo'shing)" && exit 1
          fi
          case "$TARGET" in
            /|/var/www|/var/www/|/home|/root|/etc*|/usr*)
              echo "::error::DEPLOY_PATH ($TARGET) xavfli — alohida frontend papka bo'lsin" && exit 1 ;;
          esac
          test -d "$TARGET" || { echo "::error::$TARGET papkasi yo'q"; exit 1; }
          rsync -a --delete dist/ "$TARGET"/
